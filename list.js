class ListView extends NView {

    constructor() {
        super();
    }

    build(me, target) {
        //
        // const y = D();
        // y.query = (q) => {
        //     console.log('query', q, me.mem);
        //     me.get(q, x => y.append($('<h1>' + x + '</h1>')));
        // };
        // target.html(y);
        // return y;
    }
}
